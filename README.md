# Nucel Helm Charts

Helm chart repository for Nucel products.

```bash
helm repo add nucel https://charts.nucel.dev
helm repo update
helm search repo nucel
```

Charts:
- [agent-operator](https://artifacthub.io/packages/helm/nucel/agent-operator) — Kubernetes operator for autonomous AI agents

