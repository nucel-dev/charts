{{- if .Values.surrealdb.deploy }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: surrealdb
  namespace: {{ include "nucel.namespace" . }}
  labels:
    {{- include "nucel.labels" . | nindent 4 }}
spec:
  serviceName: surrealdb
  replicas: 1
  selector:
    matchLabels:
      {{- include "nucel.surrealdbLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nucel.surrealdbLabels" . | nindent 8 }}
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      containers:
        - name: surrealdb
          image: {{ .Values.surrealdb.image }}
          args:
            - start
            - --log
            - info
            - --user
            - $(DB_USER)
            - --pass
            - $(DB_PASS)
            {{- if .Values.surrealdb.tikvEndpoint }}
            - tikv://{{ .Values.surrealdb.tikvEndpoint }}
            {{- else }}
            - file:/data/nucel.db
            {{- end }}
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "nucel.secretName" . }}
                  key: NUCEL_DB_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ include "nucel.secretName" . }}
                  key: NUCEL_DB_PASS
          ports:
            - containerPort: 8000
              name: ws
          {{- if not .Values.surrealdb.tikvEndpoint }}
          volumeMounts:
            - name: data
              mountPath: /data
          {{- end }}
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            {{- toYaml .Values.surrealdb.resources | nindent 12 }}
  {{- if not .Values.surrealdb.tikvEndpoint }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- if .Values.storage.storageClassName }}
        storageClassName: {{ .Values.storage.storageClassName }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.surrealdb.storage }}
  {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: surrealdb
  namespace: {{ include "nucel.namespace" . }}
  labels:
    {{- include "nucel.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "nucel.surrealdbLabels" . | nindent 4 }}
  ports:
    - port: 8000
      targetPort: 8000
      name: ws
{{- end }}
